#include <iostream>
#include <string>

using namespace std;

class GameOfLife {
public:
  void startGame();
  void update();
  void endGame();
  void userQueerE();
  void pause();

//  void makeRandomGrid(int gameModeNum, int numRows, int numColumn, double percentAlive);
  //void printGrid(Cell& cells);
private:
  // N/A
};
